import { formatMoney } from "../utils/money.js";
import { escapeHtml } from "../utils/html.js";
export function renderCart({ open, items, summary }) {
  if (!open || !items.length) return "";
  return `<div class="rp-cart-overlay" aria-hidden="false"><button class="rp-cart-overlay__backdrop" type="button" data-close-cart aria-label="Fechar pedido"></button><section class="rp-cart-sheet" role="dialog" aria-modal="true" aria-labelledby="rp-cart-title" data-cart-sheet><div class="rp-cart-sheet__handle" aria-hidden="true"></div><header class="rp-cart-sheet__head"><div><p class="rp-cart-sheet__eyebrow">Pedido</p><h2 id="rp-cart-title">Seu pedido</h2></div><button type="button" class="rp-cart-sheet__close" data-close-cart aria-label="Fechar pedido">×</button></header><div class="rp-cart-sheet__items">${items.map(({ product, quantity }) => `<article class="rp-cart-item"><div class="rp-cart-item__copy"><strong>${escapeHtml(product.nome || "Produto")}</strong><span>${formatMoney((Number(product.preco_centavos) || 0) * quantity)}</span></div><div class="rp-cart-item__stepper" aria-label="Quantidade de ${escapeHtml(product.nome || "produto")}"><button type="button" data-cart-delta="-1" data-product-id="${escapeHtml(product.id)}" aria-label="Remover uma unidade">−</button><strong>${quantity}</strong><button type="button" data-cart-delta="1" data-product-id="${escapeHtml(product.id)}" ${quantity >= Number(product.estoque || 0) ? "disabled" : ""} aria-label="Adicionar uma unidade">+</button></div></article>`).join("")}</div><div class="rp-cart-total"><span>Total</span><strong>${formatMoney(summary.totalCents)}</strong></div><div class="rp-cart-sheet__actions"><button class="rp-btn rp-btn--primary rp-cart-sheet__primary" type="button" data-start-checkout>Continuar para pagamento <span aria-hidden="true">›</span></button><button class="rp-btn rp-btn--ghost" type="button" data-close-cart>Continuar comprando <span aria-hidden="true">›</span></button></div></section></div>`;
}
